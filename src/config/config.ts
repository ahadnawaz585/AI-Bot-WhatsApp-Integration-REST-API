import { Config } from "../types/config";

const config: Config = {
    startMessage:  "hi mona",
    endMessage:  "bye mona",
    prefixes: ["!gemini", "!stability", "!vision"]
};

export default config;
